// Funções anônimas e arrow function
const somar = function(v1, v2) {
    console.log(v1 + v2)
}
somar(5,2);

const multiplicar = (v1, v2) => {
    console.log(v1 * v2);
}
multiplicar(5,2);

const dividir = (v1, v2) => console.log(v1 / v2);
dividir(10, 2);