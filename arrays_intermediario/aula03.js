// Array foreach
const lista = ['João', 'Zé', 'Maria', 'Raimunda']; 

lista.forEach((item, i, arr) => {
    console.log(item)
});

const frutas = [
    'maçã',
    'uva',
    'laranja',
    'goiaba'
];

frutas.forEach((item) => console.log(item));